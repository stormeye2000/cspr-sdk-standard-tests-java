plugins {
    id 'java-library'
}

apply plugin: 'java'

repositories {
    mavenCentral()
}

dependencies {
    implementation('network.casper:casper-java-sdk') {
        version {
            branch = 'main'
        }
    }

    implementation "io.cucumber:cucumber-java:${cucumberVersion}"

    implementation "com.syntifi.crypto:crypto-key-ed25519:${cryptokeyVersion}"
    implementation "com.syntifi.crypto:crypto-key-secp256k1:${cryptokeyVersion}"


    testImplementation "org.junit.jupiter:junit-jupiter:${jupiterVersion}"
    testRuntimeOnly "org.junit.jupiter:junit-jupiter:${jupiterVersion}"
    implementation "dev.oak3:sbs4j:${sbs4jVersion}"
    testRuntimeOnly files('assets')
}

configurations {
    cucumberRuntime {
        extendsFrom testImplementation
    }
}

task cucumberCli() {
    dependsOn assemble, testClasses
    doLast {
        javaexec {
            main = "io.cucumber.core.cli.Main"
            classpath = configurations.cucumberRuntime + sourceSets.main.output + sourceSets.test.output
            args = [
                    '--plugin', 'pretty',
                    '--plugin', 'html:target/cucumber-report.html',
                    '--glue', 'com.stormeye.evaluation',
                    '--glue', 'com.casper.sdk',
                    'src/test/resources/features']
        }
    }
}

test {
    useJUnitPlatform()
}